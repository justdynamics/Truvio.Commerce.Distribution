{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://github.com/Justinvolved/Truvio.Commerce.Serializer.BaselineUpdater/layers/layer.schema.json",
  "title": "Truvio distribution layer manifest (layer.json)",
  "description": "Contract schema for layers/<name>/layer.json. Supersedes the historical packs/pack.schema.json (v3.0 Distribution). Validated at the gate via Test-Json -SchemaFile with -ErrorAction Stop (MANDATORY - a broken schema must THROW, never vacuously pass). Digit classes are written as [0-9]: a backslash-d escape is invalid JSON and would make this whole schema unparseable. A layer ships serializer-shaped DB content (replace/ and/or merge/ mode trees) and/or a DW-filesystem disk overlay (files/). Additions bind ONLY to the base contract (layers/base/base.contract.json), never to each other.",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "name",
    "version",
    "kind",
    "swiftVersion"
  ],
  "properties": {
    "name": {
      "type": "string",
      "pattern": "^[a-z0-9-]+$",
      "description": "Layer identity. Reused in filesystem paths and release tags - lowercase letters, digits, and hyphen only (identifier-allowlist compatible)."
    },
    "version": {
      "type": "string",
      "pattern": "^[0-9]+\\.[0-9]+\\.[0-9]+$",
      "description": "Semver release version; component of the layers/<name>/<version> annotated tag."
    },
    "kind": {
      "enum": [
        "base",
        "catalog",
        "feature",
        "theme",
        "sample-data",
        "surface"
      ],
      "description": "Layer classification (prefix = kind; the layer dir name carries the kind as its prefix — catalog-*, feature-*, surface-*, theme-*; base and sample-data are singletons). base = the one privileged layer every edition builds on (ships the base contract). catalog = shop catalog content (groups/products/prices). feature = a customization tier bundle (may carry src/ .cs). theme = disk-overlay-only presentation (files/ mirroring System/Styles per SPEC-06; no DB data), applied via themes[] — the distribution ships ONE default theme (theme-default: the neutral starting point every customer re-skin overwrites). sample-data = optional demo content (orders/users/contract prices). surface = what a frontend leg needs (e.g. headless content + itemtypes + repositories)."
    },
    "swiftVersion": {
      "type": "string",
      "pattern": "^[0-9]+\\.[0-9]+\\.[0-9]+$",
      "description": "The single supported Swift version this layer claims compatibility with (the rolling latest). The gate PROVES it against the configured version - this is a claim, never trusted."
    },
    "repositoryName": {
      "type": "string",
      "pattern": "^[a-zA-Z0-9._-]+$",
      "description": "Convention: the storefront search repository the layer's eCom_ProductCatalog surfaces (e.g. the Quick Order validation feed) query. The authority is base.contract.json 'repositories': 'ProductsFrontend' (Products.index + Products.query + Products.facets), which the host's Swift design package supplies - no layer here ships it. A layer declaring a repository the host does not carry renders HTTP 200 with an in-page Lucene 'numHits must be > 0' error, so the sku-validation probe (which exercises this feed) is what catches the mismatch, never an HTTP-status check."
    },
    "csLedger": {
      "type": "array",
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": [
          "file",
          "classification",
          "registration"
        ],
        "properties": {
          "file": {
            "type": "string",
            "description": "Path of the .cs source file relative to the layer root (e.g. src/NoopFixtureSubscriber.cs)."
          },
          "classification": {
            "type": "string",
            "description": "Free-form purpose label (e.g. fixture-noop, checkout-handler)."
          },
          "registration": {
            "enum": [
              "assembly-scan",
              "addin-attribute-config-row",
              "notification-subscriber"
            ],
            "description": "How DW discovers this type. addin-attribute-config-row entries MUST have at least one matching configRows entry (cross-field rule enforced at activation, not expressible in JSON Schema)."
          }
        }
      },
      "description": "OPTIONAL per-.cs-file ledger. Only feature layers carrying src/ populate it; every file under src/ must appear here (ledger-completeness checked at publish time). Layers with no custom code (base, catalog, theme, sample-data, most surfaces) omit the field or ship an empty array."
    },
    "configRows": {
      "type": "array",
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": [
          "table",
          "where",
          "description"
        ],
        "properties": {
          "table": {
            "type": "string",
            "pattern": "^[a-zA-Z0-9._-]+$",
            "description": "Table name - identifier charset only (letters, digits, dot, hyphen, underscore); re-validated against the identifier allowlist before any SQL interpolation."
          },
          "where": {
            "type": "string",
            "pattern": "^ *[A-Za-z0-9_]+ *= *'[a-zA-Z0-9 ._-]*'( +AND +[A-Za-z0-9_]+ *= *'[a-zA-Z0-9 ._-]*')* *$",
            "description": "SQL WHERE predicate restricted to the exact structural shape <Column> = '<value>' optionally AND-joined (uppercase AND). The old charset-only pattern admitted OR 1=1 and -- comment tails, letting a layer make its own assert vacuously PASS (WR-05). Values carry no quote character so the literal can never be escaped. Consumers re-validate at runtime before any interpolation."
          },
          "description": {
            "type": "string",
            "description": "Human-readable purpose of the declared row."
          }
        }
      },
      "description": "OPTIONAL rows Invoke-LayerAssert verifies exist after activation (SQL EXISTS probes). Layers touching no config rows (theme, most catalog/sample-data) omit the field."
    },
    "fragmentModes": {
      "type": "array",
      "items": {
        "enum": [
          "replace",
          "merge"
        ]
      },
      "description": "Which SerializeRoot mode trees the layer ships. replace = source-wins (was 'deploy'), merge = destination-wins field-level (was 'seed'). Only listed modes are POSTed at activation. Theme layers (files/ only, no DB data) omit the field."
    },
    "fragmentTables": {
      "type": "array",
      "items": {
        "type": "string",
        "pattern": "^[a-zA-Z0-9_]+$"
      },
      "description": "OPTIONAL declared table footprint - input to the static key-collision check against the base contract. Strict identifier charset (letters, digits, underscore ONLY - no dot/hyphen): fragment tables feed the activation manifest's rowsInserted, whose teardown DELETE enforces this exact allowlist (Invoke-LayerDeactivation) - a looser charset here would create an activated state that can never be torn down (WR-03). Layers shipping no SqlTable rows (theme) omit the field."
    },
    "fragmentContent": {
      "type": "array",
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": [
          "areaId",
          "path"
        ],
        "properties": {
          "areaId": {
            "type": "integer",
            "description": "Area id the declared page tree lives under (matches the fragment manifest Content entry areaId; surface-swift anchors area 3 = 'Swift 2')."
          },
          "path": {
            "type": "string",
            "pattern": "^[a-zA-Z0-9 ._/-]+$",
            "description": "Page path under the area (matches the fragment manifest Content entry path). Charset-constrained: letters, digits, space, dot, underscore, slash, hyphen - no quote characters. Traversal sequences ('..') are additionally rejected at activation (T-07-03)."
          }
        }
      },
      "description": "OPTIONAL declared content footprint - the fragmentTables analog for pages (WR-03 philosophy: declared footprint gates what may land). Every providerType=Content entry in a shipped fragment manifest must match a declared entry (areaId + path) or activation fails before any POST. Layers shipping no _content omit this field."
    },
    "sql": {
      "type": "array",
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": [
          "file",
          "mode",
          "phase",
          "order",
          "description"
        ],
        "properties": {
          "file": {
            "type": "string",
            "pattern": "^_sql/[a-zA-Z0-9._-]+\\.sql$",
            "description": "Path to the script relative to the layer's mode tree (<layer>/<mode>/). Charset-constrained; traversal sequences ('..') are rejected by the pattern."
          },
          "mode": {
            "enum": [
              "replace",
              "merge"
            ],
            "description": "Which mode tree the script is staged under. It selects the staging directory only: a loose script is executed by the applier, never by the serializer's manifest-driven deserialize."
          },
          "phase": {
            "enum": [
              "before-host-start",
              "after-replace-deserialize",
              "after-merge-deserialize"
            ],
            "description": "When the applier must execute the script relative to the composition run. before-host-start = the rows must exist before Dynamicweb caches state at startup (identity/permission rows). after-replace-deserialize = the script FKs against framework rows the base replace tree lands (shop, currency, country). after-merge-deserialize = the script depends on merge-tree rows."
          },
          "order": {
            "type": "integer",
            "minimum": 1,
            "description": "Execution order within a phase, ascending. Distinct per phase within a layer."
          },
          "description": {
            "type": "string",
            "description": "What the script inserts, in one line. Read by a human deciding whether the layer belongs in an edition."
          },
          "idempotent": {
            "type": "boolean",
            "description": "The script is safe to re-run against an already-seeded database (IF NOT EXISTS upserts or DELETE+INSERT on its own reserved keys). An applier may refuse to re-run a script that does not declare this."
          },
          "transactional": {
            "type": "boolean",
            "description": "The script wraps its work in a transaction with SET XACT_ABORT ON, so a mid-script failure leaves no partial seed."
          },
          "requiresHostRestart": {
            "type": "boolean",
            "description": "The host must be restarted after the script runs for the rows to become first-class (Dynamicweb's startup catalog/identity caches do not see rows inserted underneath a running host)."
          },
          "sqlcmdVariables": {
            "type": "array",
            "items": {
              "type": "object",
              "additionalProperties": false,
              "required": [
                "name",
                "description"
              ],
              "properties": {
                "name": {
                  "type": "string",
                  "pattern": "^[A-Za-z][A-Za-z0-9_]*$",
                  "description": "sqlcmd variable name the script references as $(Name)."
                },
                "secret": {
                  "type": "boolean",
                  "description": "The value is a credential the caller supplies at apply time. Secret values are never repo content and are never logged."
                },
                "valueShape": {
                  "enum": [
                    "dw-password-hash"
                  ],
                  "description": "The shape of the value the script receives. dw-password-hash: the variable carries the Dynamicweb platform password hash AccessUserPassword stores (lowercase hex of SHA512(UTF8(password + \"DwSecret\")), 128 characters), never the plaintext. The caller supplies the plaintext and the applier hashes it once; a script receiving any other shape must refuse before writing. Every sqlcmd variable whose name contains 'password' MUST declare it."
                },
                "description": {
                  "type": "string",
                  "description": "What the caller must supply."
                }
              }
            },
            "description": "sqlcmd variables the script requires. An applier that cannot supply every declared variable must fail before executing the script, not substitute a blank."
          }
        }
      },
      "description": "OPTIONAL declared loose T-SQL scripts the layer ships under <mode>/_sql/*.sql, and the discovery contract for them. The serializer manifest has NO provider for a whole script: its only providerTypes are 'Content' and 'SqlTable', and SqlTable is row-per-YAML under _sql/<Table>/<key>.yml. A loose .sql therefore carries no <mode>-manifest.json entry and the manifest-driven deserialize never executes it, so a composer that reads only the manifests stages the file and silently runs nothing. This array is what a composer reads instead: it names each script, when to execute it, in what order, and what the caller must supply. A layer that ships a loose .sql under a mode tree MUST declare it here. The fragmentTables/fragmentContent/files analog for executable scripts.",
      "minItems": 1
    },
    "files": {
      "type": "array",
      "items": {
        "type": "string",
        "pattern": "^[a-zA-Z0-9 ._/-]+$"
      },
      "description": "OPTIONAL declared disk-overlay footprint - DW-filesystem-rooted paths the layer's files/ tree overlays (e.g. System/Styles/ColorSchemes/..., Templates/Designs/Swift-v2/Assets/..., Files/Images/Icons/...). The files/ analog of fragmentTables/fragmentContent: theme (SPEC-06) and surface layers declare here what disk paths they own. Charset-constrained (no quote characters); traversal sequences ('..') rejected at activation. Disk FKs stay on disk; DB bindings stay in the serializer (SPEC-06 boundary)."
    },
    "repositories": {
      "type": "array",
      "items": {
        "type": "string",
        "pattern": "^[a-zA-Z0-9 ._/+-]+$"
      },
      "description": "OPTIONAL declared REPOSITORY footprint - the repositories/ analog of files[], in the identical vocabulary: composed-site Files-relative paths (System/Repositories/<Repo>/<Name>.index, .query, .facets, and the repository's Build+Index.task), never layer-relative ones. The layer stages repositories/<Repo>/<Name>.ext to System/Repositories/<Repo>/<Name>.ext. Declared so the staging set is DERIVABLE rather than a folder convention a consumer is expected to walk: a consumer that stages from the declared arrays stages these too, a manifest audit can see them, and a definition retired from the tree is detectable as retired because there is a previous declaration to diff against. Required whenever the layer ships a non-empty repositories/ tree; declare [] to state positively that it ships none. .gitkeep is never listed. Charset-constrained, traversal sequences ('..') rejected - same discipline as files[], plus '+', which Dynamicweb's fixed task file name Build+Index.task requires."
    },
    "itemtypes": {
      "type": "array",
      "items": {
        "type": "string",
        "pattern": "^[a-zA-Z0-9 ._/-]+$"
      },
      "description": "OPTIONAL declared ITEM TYPE footprint - the itemtypes/ analog of files[], in the identical vocabulary: composed-site Files-relative paths (System/Items/ItemType_<Name>.xml), never layer-relative ones. The layer stages itemtypes/ItemType_<Name>.xml to System/Items/ItemType_<Name>.xml. Same reason as repositories[]: without it an item type exists only as a folder convention, no manifest check can see it, and a retired one cannot be detected. Note that an itemtype placeholder is proven by the REGISTRATION leg, not by bytes - activation rewrites the XML on the host - so this array declares what is STAGED, which is what a staging audit compares against disk. Required whenever the layer ships a non-empty itemtypes/ tree; declare [] to state positively that it ships none. .gitkeep is never listed. Charset-constrained, traversal sequences ('..') rejected - same discipline as files[]."
    },
    "placeholders": {
      "type": "array",
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": [
          "path",
          "kind",
          "why"
        ],
        "properties": {
          "path": {
            "type": "string",
            "pattern": "^[a-zA-Z0-9 ._/-]+$",
            "description": "The placeholder's path FILES-RELATIVE ON THE COMPOSED SITE - where the composer stages the artefact under the host's wwwroot/Files/, which is the same path the gate asserts, and NOT the path inside the layer directory. So a repositories/ artefact declares System/Repositories/TruvioCommerce/Products.query, an itemtypes/ XML declares System/Items/ItemType_TC_AnchorNav.xml, and a files/ overlay declares Templates/Designs/Swift-v2/Custom/default_custom.js with no files/ prefix - identical in form to files[]. A layer-relative path does not resolve and the gate reports the entry ABSENT (v5 e2e, DW 10.28.10). Charset-constrained, traversal sequences ('..') rejected - same discipline as files[]."
          },
          "kind": {
            "enum": [
              "css",
              "js",
              "template",
              "repository",
              "asset",
              "itemtype"
            ],
            "description": "The artefact class, and the proof that class OWES before a placeholder of it counts as proven. Owed: css -> lint + served-200; js -> served-200 + parses; template -> a real GET of a page that renders it, asserted on body content and never on status (a Razor error is an HTTP-200 compile dump); repository -> XML parse + Type= resolution on the STAGED artefact; asset -> present + served-200; itemtype -> the registration leg (parsed fields on the host, not bytes - activation rewrites the XML). What the Foundry gate RUNS is narrower. The placeholder leg checks present, the parse claim (XML for repository, JSON, the CSS sentinel) and inert (a byte diff against the pinned Swift overlay, SKIP where no origin exists); itemtype registration is the item-type leg. The placeholder leg makes no HTTP request, so served-200, lint, the template body GET and Type= resolution are NOT run, and a green placeholder leg is never evidence that a template renders or that a js or asset entry is served. Until the owed proof runs, prove a template or js placeholder with an asserts.behaviorProbes entry on a page that loads it."
          },
          "why": {
            "type": "string",
            "description": "One line: what a consumer fills this in for. Read by the person deciding whether to edit it or create something new - the whole point of a placeholder is that a re-skin is an edit, never a create."
          }
        }
      },
      "description": "OPTIONAL declared PLACEHOLDER footprint - present-but-inert hooks the layer ships so a re-skin is an edit and never a create. A placeholder is (a) present on disk, (b) neutral - no brand, no lorem, (c) documented in-file with its fill-in recipe, (d) inert - it adds nothing to render output until a consumer opts in, and (e) replaceable by a brand overlay without editing anything else. Declaring the footprint here gives the gate a declared set instead of a hand-maintained list: the Foundry placeholder leg asserts present, parsed and inert over it (see kind for the proof each class owes and the part that leg does not run). Neutral (b), documented (c) and replaceable (e) are authoring rules that leg does not assert. Every entry that comes from the layer's files/ tree must also appear in files[], with the identical Files-relative spelling."
    },
    "asserts": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "behaviorProbes"
      ],
      "properties": {
        "behaviorProbes": {
          "type": "array",
          "items": {
            "type": "object",
            "additionalProperties": false,
            "required": [
              "kind",
              "path"
            ],
            "properties": {
              "kind": {
                "type": "string",
                "description": "Probe kind. Supported registry (07-02, extended 08-01, 09-01, scaffolding-only round): marker-file, http-body-contains, authenticated-body-contains, cart-price, checkout-recurring, bom-cart-lines, sku-validation. An unknown kind produces a fail-closed FAIL row at assert time - never a silent skip."
              },
              "path": {
                "type": "string",
                "description": "marker-file: host-relative artifact path the probe checks. http-body-contains / authenticated-body-contains: frontend path the probe GETs. cart-price: frontend path receiving the cartcmd=addmulti POST. checkout-recurring: the layer Subscribe page - addmulti POST target AND render-proof GET AND checkout POST target. bom-cart-lines: the layer Kit Configurator page - the kind appends ?ProductID={productId} internally for the render proof, then POSTs cartcmd=add here (path-only - a query string is rejected, the charset never loosens)."
              },
              "pattern": {
                "type": "string",
                "description": "Regex the response body must match (http-body-contains / authenticated-body-contains). Charset-validated (printable ASCII) and compile-checked at runtime BEFORE any request (T-07-06). Kind-conditional requiredness is enforced at runtime by the probe functions (fail-closed FAIL rows) - the section-6 registration-asymmetry precedent for rules JSON Schema cannot express."
              },
              "cartPath": {
                "type": "string",
                "description": "cart-price: frontend cart path GET after the addmulti POST (e.g. /swift-2/cart/). Required at runtime for kind cart-price (fail-closed FAIL row when absent). bom-cart-lines: the layer cart render path - path-only (query string rejected), validated when present; the SQL leg is the arbiter so it is not GET at assert time."
              },
              "productId": {
                "type": "string",
                "description": "cart-price / checkout-recurring: product id added via the numbered addmulti fields (ProductId1); cart-price additionally locates it in the cart body. bom-cart-lines: the BOM parent product id - appended to the render URL as ?ProductID={productId}, POSTed as the add-POST ProductId, AND matched against parent.OrderLineProductId in the SQL leg. Conservative charset re-validated at runtime before any request or SQL interpolation (T-06-08)."
              },
              "quantity": {
                "type": "integer",
                "minimum": 1,
                "description": "cart-price / checkout-recurring: line quantity POSTed as Quantity1 (for cart-price it must reach the declared tier threshold to prove qty-break pricing)."
              },
              "expectedAmount": {
                "type": "number",
                "description": "cart-price only: expected unit price on the cart line for productId. Matched locale-tolerantly at runtime (dot or comma decimal separator, optional thousand separators). A cart showing only the base price FAILs with the observed price in the detail."
              },
              "paymentMethodId": {
                "type": "string",
                "description": "checkout-recurring only: payment method id (e.g. PAY2) POSTed as EcomCartPaymethodID at checkout. Conservative identifier charset re-validated at runtime before any request (T-08-02). Required at runtime for kind checkout-recurring (fail-closed FAIL row when absent)."
              },
              "shippingMethodId": {
                "type": "string",
                "description": "checkout-recurring only: shipping method id (e.g. SHIP9) POSTed as EcomCartShippingmethodID at checkout. Conservative identifier charset re-validated at runtime before any request (T-08-02). Required at runtime for kind checkout-recurring (fail-closed FAIL row when absent)."
              },
              "checkoutStepIndex": {
                "type": "integer",
                "minimum": 1,
                "description": "checkout-recurring only: the CartV2.GotoStep{i} submit-button index of the step whose IsCheckout=true, POSTed as CartV2.GotoStep{checkoutStepIndex}. CartV2.GotoStep{i} is 0-BASED - index 1 targets the SECOND step, so for a paragraph whose Step2IsCheckout=true declare checkoutStepIndex 1, NEVER 2 (the 08-03 live failure was exactly this off-by-one). Declared data - a 3-step vs 5-step paragraph fallback is a layer.json edit, never a harness change. Required at runtime for kind checkout-recurring (fail-closed FAIL row when absent or < 1)."
              },
              "selections": {
                "type": "array",
                "items": {
                  "type": "object",
                  "additionalProperties": false,
                  "required": [
                    "group",
                    "product"
                  ],
                  "properties": {
                    "group": {
                      "type": "string",
                      "description": "bom-cart-lines: the BOM SLOT id = EcomProductItems.ProductItemId (NOT the EcomGroups id - the stock Swift-v2_ProductBom radio name=\"@group.Id\" is the BOM item/slot id, 09-03 live finding). Posted as the add-POST field NAME, and matched against the child line's native OrderLineBOMItemId in the per-child disambiguation SQL leg. Conservative identifier charset re-validated at runtime before it enters the POST body / SQL literal (T-09-02)."
                    },
                    "product": {
                      "type": "string",
                      "description": "bom-cart-lines: the NON-default child product id selected in that slot - posted as the add-POST field VALUE (<slot>=<product>) and matched against the child line's OrderLineProductId in the disambiguation leg (proves the non-default selection was consumed, not the slot default). Conservative charset re-validated at runtime (T-09-02)."
                    }
                  }
                },
                "description": "bom-cart-lines only: one {group, product} per BOM slot the add POST selects, posted as <group>=<product> alongside cartcmd=add. Required at runtime for kind bom-cart-lines (fail-closed FAIL row when absent or empty) - the kind-conditional requiredness JSON Schema cannot express is enforced by the probe (section-6 registration-asymmetry precedent)."
              },
              "expectedChildLines": {
                "type": "integer",
                "minimum": 1,
                "description": "bom-cart-lines only: the number of BOM child lines the add POST must create. The SQL leg PASSes on COUNT >= expectedChildLines (never = exact - 12c cycle legs accumulate rows). Required at runtime for kind bom-cart-lines (fail-closed FAIL row when absent or < 1)."
              },
              "fieldStamp": {
                "type": "string",
                "description": "bom-cart-lines only: OPTIONAL OrderLineFieldValues key the PK3-02 slot-stamp subscriber writes (e.g. PackBomSlot). ADDITIVE / best-effort as of 09-03: reported informationally in the PASS detail, NEVER a hard gate - PK3-02 per-child disambiguation is proven by the native OrderLineBOMItemId (engine-populated), so the subscriber stamp is enrichment only (its non-persistence is a documented morning investigation item). Conservative identifier charset re-validated at runtime before the SQL LIKE interpolation (T-09-02)."
              },
              "renderProof": {
                "type": "boolean",
                "description": "bom-cart-lines only: OPTIONAL, default true. When true the probe GETs {path}?ProductID={productId} and requires the js-product-bom-configurator marker + >=1 radio group (NOT a compile-error dump) BEFORE the add POST - the Pitfall-1 UI-wiring guard for hosts whose storefront product index is provisioned. Set false when the storefront render is out of the harness's scope (09-03: the harness clean-room does not provision the ProductsFrontend index, so no eCom_ProductCatalog app renders any product; the BOM cart behavior - non-default child lines keyed by native OrderLineBOMItemId - is the harness-provable requirement, and the storefront configurator render becomes a real-Swift-host UAT item)."
              },
              "sku": {
                "type": "string",
                "pattern": "^[a-zA-Z0-9 ._-]+$",
                "description": "sku-validation only: a layer-owned ProductNumber the Quick Order validation feed must resolve. The probe GETs {path}?feed=true&redirect=false&isVariant=true,false&q={sku} (query built internally; path never accepts ?/=/&). Conservative ProductNumber charset re-validated at runtime before the request (T-06-08). Required at runtime for kind sku-validation (fail-closed FAIL row when absent)."
              },
              "expectProductNumber": {
                "type": "string",
                "pattern": "^[a-zA-Z0-9 ._-]+$",
                "description": "sku-validation only: OPTIONAL - the ProductNumber the feed article must carry (default = sku). Set when the queried SKU differs from the expected resolved ProductNumber. Same charset guard as sku."
              },
              "requiresFixtures": {
                "type": "array",
                "minItems": 1,
                "uniqueItems": true,
                "items": { "type": "string", "pattern": "^[A-Za-z0-9 ._-]+$" },
                "description": "OPTIONAL, edition-level opt-in. The sample-data ids this probe's subject resolves through - rows the sample-data layer ships, each one a subject named in base.contract.json sampleData.guaranteedRows, which is what the probe actually binds to. behaviorProbes are LAYER asserts, so they run for every edition composing the layer; a probe that declares requiresFixtures runs ONLY where the composition sets sampleData true, and is recorded as a loud SKIP row naming those ids and a proving edition everywhere else. A probe is NEVER silently dropped: the SKIP row lands in pack-assert.json alongside the PASS rows. Declare it whenever the probe names an id the sample-data layer owns; omit it for probes that only assert page markup the feature layer itself ships."
              }
            }
          },
          "description": "Behavior probes Invoke-LayerAssert runs after activation. Authenticated kinds (authenticated-body-contains, cart-price, checkout-recurring) ride the canonical seeded buyer session (gate config packs.buyerUsername + the buyerPassword secret from git-ignored config/gate-secrets.local.json, falling back to the documented demo default) and FAIL closed when credentials are absent - never a silent skip."
        },
        "criticalPaths": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "Optional frontend paths that must respond 2xx after activation. Enforced by Invoke-LayerAssert (WR-07): one anonymous GET per path, PASS on 2xx, FAIL row otherwise - never silently skipped."
        }
      },
      "description": "OPTIONAL behavior assertion declarations consumed by Invoke-LayerAssert."
    },
    "knownCycleLimitations": {
      "type": "array",
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": [
          "probe",
          "path",
          "phase",
          "reason"
        ],
        "properties": {
          "probe": {
            "enum": [
              "marker-file",
              "http-body-contains",
              "authenticated-body-contains",
              "cart-price",
              "checkout-recurring",
              "bom-cart-lines",
              "sku-validation"
            ],
            "description": "The behavior-probe kind of the exempted assert (matches an asserts.behaviorProbes[].kind)."
          },
          "path": {
            "type": "string",
            "pattern": "^/[a-zA-Z0-9/._-]*$",
            "description": "The frontend path of the exempted assert (matches the asserts.behaviorProbes[].path). Rooted, conservative charset - no query characters."
          },
          "phase": {
            "enum": [
              "behavior-cycle"
            ],
            "description": "The gate phase in which the exemption applies. ONLY 'behavior-cycle' (the deactivate->reactivate cycle leg) is honored - a first-activation FAIL, a config-row-cycle FAIL, or any other cycle assert is NEVER exempted."
          },
          "reason": {
            "type": "string",
            "description": "Human-readable tracked-known-issue rationale, surfaced VERBATIM in the gate's KNOWN-LIMITATION output (COMPATIBILITY layer rows + pipeline log)."
          }
        }
      },
      "description": "OPTIONAL. Layer-declared known cycle-limitations: tracked-but-unfixed cycle edges the gate records as KNOWN-LIMITATION (WARN) instead of FAIL. Matched TIGHTLY by layer + probe + path + phase; a NON-declared cycle FAIL still hard-fails the gate. Surfaced loudly (never a silent skip). Layers with no known limitations omit the field."
    },
    "provenOn": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "swiftVersion",
        "gateRunId"
      ],
      "properties": {
        "swiftVersion": {
          "type": "string",
          "pattern": "^[0-9]+\\.[0-9]+\\.[0-9]+$",
          "description": "The Swift version the gate last proved this layer against."
        },
        "gateRunId": {
          "type": "string",
          "pattern": "^[0-9]{8}-[0-9]{6}$",
          "description": "The gate run id (runs/<ts>) that produced the proof - the same id carried into the annotated release tag."
        }
      },
      "description": "OPTIONAL attestation stamped by the gate on a PASS (never hand-authored as trusted): the swiftVersion + gate run id this layer was last proven against. Feeds the annotated layers/<name>/<semver> tag."
    },
    "costHints": {
      "type": "object",
      "description": "OPTIONAL free-form advisory metadata (e.g. activation cost, host-rebuild required, expected row counts). Never gates activation; consumed only for planning/reporting."
    },
    "customCode": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "compileOptional",
        "withoutCompile",
        "withCompile"
      ],
      "properties": {
        "compileOptional": {
          "type": "boolean",
          "description": "TRUE when the layer's src/ providers are OPTIONAL to compile: the layer installs and delivers a documented headline of behavior as pure data (no custom code), and compiling the sources only ADDS behavior. FALSE would mean the layer is inert without its code. The base layer stays zero-custom-code (constraint unchanged); a FEATURE layer MAY ship declared, compile-optional providers via this block (decision D3)."
        },
        "sources": {
          "type": "array",
          "items": {
            "type": "string",
            "pattern": "^[a-zA-Z0-9 ._/-]+$"
          },
          "description": "The src/ .cs file paths this declaration covers (a subset of csLedger[].file). Charset-constrained; no quote characters."
        },
        "withoutCompile": {
          "type": "string",
          "description": "The behavior the layer GUARANTEES with the sources NOT compiled (pure data on the native platform) — the zero-code headline. e.g. contract pricing enforced by the stock DefaultPriceProvider."
        },
        "withCompile": {
          "type": "string",
          "description": "The additional behavior UNLOCKED when the sources are compiled into the Swift solution and registered (e.g. assembly-scan). Must be additive: compiling never removes or breaks the withoutCompile guarantee."
        },
        "compileStep": {
          "type": "string",
          "description": "OPTIONAL human/skill-readable pointer to the opt-in compile recipe (the dynamicweb-skills feature-layer compile step)."
        }
      },
      "description": "OPTIONAL declaration that a feature layer ships compile-optional custom code (decision D3). Documents what works as data-only (withoutCompile) vs after the opt-in compile (withCompile). Only feature layers carrying src/ populate it; layers with no custom code omit it. Advisory: never gates activation — the gate still compiles + proves the full behavior; this block tells downstream distribution consumers what is optional."
    }
  }
}
